# ==============================================================================
# Proxmox Infrastructure Provider — Example Environment
# ==============================================================================
# Copy this file to `.env` and replace placeholder values with your own.
# IMPORTANT: `.env` contains secrets. Do NOT commit it to version control.

# ==============================================================================
# Required — Omni settings
# ==============================================================================
# Your Omni API base URL (must use HTTPS). 
# Include the trailing slash if your setup expects it.
OMNI_API_ENDPOINT=https://omni.yourdomain.com/

# Infrastructure Provider Key
# - This key is created in the Omni web UI under:
#     Settings → Infrastructure Providers → Create Provider
# - Use the Infrastructure Provider Key (not a service-account key).
OMNI_INFRA_PROVIDER_KEY=your-infra-provider-key-here

# ==============================================================================
# Notes & Security Tips
# ==============================================================================
# - Proxmox-specific credentials (username, password, host) live in `config.yaml`.
#   Create `config.yaml` by copying `config.yaml.example` and filling the values.
# - Keep file permissions tight for `.env`: chmod 600 .env
# - Treat the provider key as a secret. Rotate it from the Omni UI if it is exposed.